# Native System

The initial system, process and pager policy, service supervision, device management, identity, VFS, update, logging, and other privileged user-space servers belong here as separate capability domains.

