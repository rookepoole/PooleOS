# PooleBoot Exit Contract

`poole-boot-exit` is the dependency-free `no_std` PBEXIT1 state machine for
the bounded final-memory-map and `ExitBootServices` sequence. It does not call
firmware, transfer to PooleKernel, or establish trust.
