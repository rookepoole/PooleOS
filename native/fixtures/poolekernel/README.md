# PooleKernel Toolchain Fixture

This empty `ET_EXEC` binary exists only to qualify the pinned `x86_64-unknown-none` compiler and linker path. It is not PooleKernel, does not share the product linker contract, and cannot satisfy an N6-N15 requirement.
